jest.unmock('../src/cli');

const cli = require('../src/cli');

describe('cli', () => {
  describe('debug', () => {
    it('console.logs only when the --verbose flag is set', () => {

    });
  })
});
